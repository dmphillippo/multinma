% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nma.R
\name{connect_baseline}
\alias{connect_baseline}
\alias{con}
\title{Specify baseline connections}
\usage{
con(
  type = c("fixed", "random"),
  studies,
  prior_baseline = NULL,
  baseline_trt = NULL
)
}
\arguments{
\item{type}{Type of connection, either "fixed" or "random".}

\item{studies}{Vector of study names.}

\item{prior_baseline}{Prior distribution for the baseline parameters of
\code{studies} when \code{type = "random"}, as a \link{nma_prior} object (see \link{priors}).}

\item{baseline_trt}{Which treatment the baseline prior distribution applies
to, when \code{type = "random"}. By default, the random baseline prior will be
placed on the network reference treatment. This should be changed if, for
example, the prior was obtained from a baseline synthesis of a different
treatment.}
}
\value{
An object of class \code{nma_connect}.
}
\description{
Helper function for the \code{connect_baseline} argument of \code{\link[=nma]{nma()}} to specify how
study baselines (intercepts) are linked, to connect a disconnected network or
single-arm studies.
}
\details{
A random baseline connection is specified with \code{type = "random"}.
This places an informative prior distribution on the baselines of studies
listed in \code{studies}, specified with the \code{prior_baseline} argument.

A fixed baseline connection is specified with \code{type = "fixed"}. This shares
the baseline parameter for studies listed in \code{studies}.
}
