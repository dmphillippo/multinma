% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bind_chains.R
\name{bind_chains}
\alias{bind_chains}
\alias{cbind.stan_nma}
\alias{cbind.mcmc_array}
\title{Bind chains}
\usage{
bind_chains(...)

\method{cbind}{stan_nma}(...)

\method{cbind}{mcmc_array}(...)
}
\arguments{
\item{...}{Multiple fitted \link{stan_nma} model objects (for \code{bind_chains()}) or
\link{mcmc_array} arrays (for \code{cbind.mcmc_array()}). Both functions also accept a
single list as input, containing the objects to combine.}
}
\value{
A \link{stan_nma} or \link{mcmc_array} object.
}
\description{
Methods to combine chains from fitted \link{stan_nma} model objects, or
\link{mcmc_array} 3D MCMC arrays. \code{cbind.stan_nma()} is an alias for
\code{bind_chains()}.
}
\details{
Objects to combine must have the same number of iterations, warmup, and
thinning, and all parameter names must match. For \code{bind_chains()}, this means
that both the model specification and input data must be the same.
}
