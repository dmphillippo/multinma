% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nma_data-class.R
\name{breaks_integer}
\alias{breaks_integer}
\title{Automatic breaks for integer scales}
\usage{
breaks_integer(prefer_n = c(5, 4, 3, 6), extend = TRUE, positive = TRUE, ...)
}
\arguments{
\item{prefer_n}{Set of preferred numbers of break points.}

\item{extend}{Extend the breaks beyond the range of the data? If \code{TRUE}
ensures that breaks are equally spaced. Default \code{TRUE}.}

\item{positive}{Create breaks for strictly positive integers? Default \code{TRUE}.}

\item{...}{Unused.}
}
\value{
Returns a function to compute breaks, as required by the \code{breaks}
argument to the \verb{scale_*_continuous} functions in \code{ggplot2}.
}
\description{
Compute automatic breaks for integer scales, making sure that the breaks are
also integers.
}
\details{
If \code{extend = TRUE} (the default), breaks may lie outside of the
range of the data, but are guaranteed to be equally spaced. If \code{extend = FALSE}, the limits of the breaks are equal to the smallest and largest
values in the data, but within this range breaks may not be equally spaced.
}
\examples{
breaks_integer()(1:9)
breaks_integer()(1:18) # extended, equally spaced
breaks_integer(extend = FALSE)(1:18) # un-extended, unequally spaced
breaks_integer(positive = FALSE)(-1:10) # allow negative values
}
